# Web-Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
jinja2==3.1.4
aiofiles==24.1.0
python-multipart==0.0.20
itsdangerous==2.2.0

# Datenbank (PostgreSQL, async für die App, sync für Alembic-Migrationen)
sqlmodel==0.0.22
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Settings / Konfiguration
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Auth (lokale Accounts)
bcrypt==4.2.1

# Feldweise Verschlüsselung sensibler Daten (app/core/crypto.py)
cryptography==44.0.0

# Dokument-Export/-Zusammenführung
docxtpl==0.19.1
pypdf==5.1.0
Pillow==10.4.0

# Tests (siehe pytest.ini, tests/) + lokales Linting (siehe ruff.toml,
# `ruff check .` - läuft bewusst nicht in einer CI-Pipeline). Versionen an
# das Schwestermodul Scandy-Lite angeglichen.
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
aiosqlite==0.20.0
ruff==0.15.22
